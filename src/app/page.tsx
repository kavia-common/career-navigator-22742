export default function HomePage() {
  return (
    <main style={{ padding: 24, fontFamily: "system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif" }}>
      <h1 style={{ margin: 0 }}>Career Navigator</h1>
      <p style={{ marginTop: 12, lineHeight: 1.5 }}>
        Next.js scaffold restored. Run <code>npm install</code> then <code>npm run dev</code>.
      </p>
      <p style={{ marginTop: 12, opacity: 0.75 }}>
        The legacy standalone prototype remains at <code>/index.html</code> in the repo, but is not used by Next.js.
      </p>
    </main>
  );
}
